version: "3.9"

services:
  bot:
    build: ./bot
    container_name: finx-bot
    volumes:
      - ./bot:/app
    restart: always

  dashboard:
    build: ./dashboard
    container_name: finx-dashboard
    ports:
      - "3000:3000"
    restart: always
